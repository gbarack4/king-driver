const instructors=[{name:'Daniel',rating:'5.0 · 89 reviews',price:75,type:'Automatic',exp:'8 years instructing',tags:['Calm & patient','Toyota Corolla'],soon:true,gender:'male'},{name:'Priya',rating:'5.0 · 64 reviews',price:79,type:'Automatic',exp:'6 years instructing',tags:['Test preparation','Mazda 3'],soon:true,gender:'female'},{name:'Michael',rating:'4.9 · 51 reviews',price:72,type:'Manual',exp:'11 years instructing',tags:['Manual specialist','Hyundai i30'],soon:false,gender:'male'}];
const cards=document.getElementById('cards');function render(filter='all'){cards.innerHTML=instructors.filter(i=>filter==='all'||(filter==='soon'&&i.soon)||(filter==='auto'&&i.type==='Automatic')||(filter==='manual'&&i.type==='Manual')||(filter==='female'&&i.gender==='female')).map(i=>`<article class="instructor"><div class="photo"><span class="available">● ${i.soon?'Available this week':'Popular choice'}</span><div class="avatar">${i.name.slice(0,2).toUpperCase()}</div><span class="price">From $${i.price}/hr</span></div><div class="card-body"><div class="card-title"><h3>${i.name}</h3><span>★ ${i.rating}</span></div><p class="meta">${i.type} · ${i.exp}</p><div class="tags">${i.tags.map(t=>`<span>${t}</span>`).join('')}</div><button class="view" onclick="document.querySelector('#booking').scrollIntoView()">View profile & availability</button></div></article>`).join('')||'<p>No instructors match this filter.</p>'}render();
document.querySelectorAll('.filters button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)}));
const toast=document.getElementById('toast');document.getElementById('searchBtn').addEventListener('click',()=>{const suburb=document.getElementById('suburb').value.trim();const transmission=document.getElementById('transmission').value;if(!suburb){toast.textContent='Please enter your suburb to find instructors.'}else if(!transmission){toast.textContent='Please select automatic or manual.'}else{toast.textContent=`Showing ${transmission.toLowerCase()} instructors near ${suburb}.`;document.querySelector('#instructors').scrollIntoView()}toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2800)});
const menu=document.querySelector('.menu'),nav=document.querySelector('#nav');menu.addEventListener('click',()=>{nav.classList.toggle('open');menu.setAttribute('aria-expanded',nav.classList.contains('open'))});document.querySelectorAll('details').forEach(d=>d.addEventListener('toggle',()=>{if(d.open)document.querySelectorAll('details').forEach(o=>{if(o!==d)o.open=false})}));
